---
- name: Install OS Tools
  ansible.builtin.apt:
    package: ["fish", "git", "jq", "make"]
    update_cache: true
    state: present

- name: Import PostgreSQL GPG Key
  ansible.builtin.apt_key:
    id: B97B0AFCAA1A47F044F244A07FCC7D46ACCC4CF8
    url: https://www.postgresql.org/media/keys/ACCC4CF8.asc
    keyring: /etc/apt/trusted.gpg.d/postgresql.gpg
    state: present

- name: Add PostgreSQL Apt Repository
  ansible.builtin.apt_repository:
    filename: postgresql
    repo: deb [arch=amd64] https://apt.postgresql.org/pub/repos/apt noble-pgdg main
    state: present

- name: Install PostgreSQL 18
  ansible.builtin.apt:
    package: ["postgresql-18"]
    update_cache: true
    state: present

- name: Set ZFS PostgreSQL permissions
  ansible.builtin.file:
    path: /var/lib/postgresql
    owner: postgres
    group: postgres
    mode: "0700"
    state: directory

- name: Start PostgreSQL
  ansible.builtin.service:
    name: postgresql
    state: started
    enabled: true
