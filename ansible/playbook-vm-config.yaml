- name: Configure VM
  hosts: all
  become: true
  timeout: 720
  roles:
    - role: zfs
    - role: tools
    - role: postgresql
    - role: sanoid
    - role: google_ops
    - role: pilot
      when: pilot_version | default('') | length > 0
